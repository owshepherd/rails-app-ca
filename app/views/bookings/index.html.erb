<%= render "shared/nav" %>

<h1>Bookings</h1>

<h1>Show page for booking</h1>


<% if !@bookings.empty? %>
  <ul>
    <% @bookings.each_with_index do |booking, index| %>
      <li>
        <%= booking.location %>
      </li>
    <% end %>
  </ul>
  <% else %>
  <p>You have no Bookings</p>
<% end %>